# 世界书激活机制说明

> 更新时间：2026-07-12
> 适用项目：`<项目根目录>`（端口 3210）

---

## 一、两级开关体系

世界书采用**两级开关**控制条目的注入：

### 1. 总开关（角色级）

- **位置**：角色编辑弹窗 → 「世界书总开关」拨片
- **值**：`on`（开启）/ `off`（关闭）
- **存储**：`characters.book_activation` 列
- **作用**：控制该角色的整个世界书是否参与注入
  - `off` → **所有**世界书条目（含常驻、关键词）均不注入
  - `on` → 由各条目的开关和激活方式决定是否注入
- **向后兼容**：旧值 `always`、`keyword` 视同为 `on`

### 2. 条目级开关（每条世界书条目）

每个条目有独立的两个控制维度：

#### a) 启用/禁用拨片（`enabled` 字段）
- 拨片关闭 → 该条目完全不参与注入（无论激活方式）
- 拨片开启 → 按"激活方式"决定何时注入

#### b) 激活方式下拉框
| 激活方式 | 字段值 | 行为 |
|----------|--------|------|
| 常驻激活 | `constant: true` | 每轮自动注入到系统提示词，不需关键词触发 |
| 关键词触发 | `constant: false, selective: false` | 用户消息/故事文本命中主关键词时注入 |
| 选择性(主键+次键) | `selective: true` | 主关键词命中 **且** 次关键词也命中时注入 |

---

## 二、注入流程详解

### 阶段 1：常驻条目 → 系统提示词注入

**触发条件**：总开关 = `on` 且 条目 `enabled = true` 且 `constant = true`

**注入位置**：系统提示词（system prompt）中，作为 `【世界书（常驻）】{内容}` 块

**代码位置**：`chat.js` ~1916行 `buildSystemPrompt` 函数内

```
系统提示词构成顺序：
  角色描述 → 性格 → 【世界书（常驻）】→ 对话级系统提示词 → 输出格式指令 → ...
```

常驻条目按原始顺序拼接，每轮都注入（无冷却）。

### 阶段 2：关键词条目 → 用户消息尾部注入

**触发条件**：总开关 = `on` 且 条目 `enabled = true` 且 `constant = false`

**注入位置**：当前用户消息末尾，作为 `【参考资料：1、条目名，内容。2、...】` 块

**代码位置**：`chat.js` ~2258行 `buildApiMessages` 函数内的关键词匹配块

#### 关键词匹配逻辑

1. **构建扫描文本**：取最近 8 条用户+助手消息 + 当前用户输入，合并为小写文本
2. **主关键词匹配**：条目的 `keys` 数组中任一关键词出现在扫描文本中 → 命中
3. **次关键词匹配**（仅选择性条目）：`selective = true` 时，`secondary_keys` 中任一也需命中
4. **冷却检查**：同一条目在 N 轮（默认 4，可配置）内不重复注入
   - 冷却轮数从世界书的 `cooldown` 字段读取
   - 检查方式：扫描最近 N 条用户消息，看是否已包含该条目的 `【参考资料】` 引用
5. **排序**：命中的条目按 `insertion_order` 升序排列后拼接

#### 正则匹配支持

次关键词支持正则表达式：如果 key 能被 `new RegExp(key)` 编译，则用正则测试；否则用 `includes` 字符串包含匹配。

---

## 三、冷却机制

- **作用**：避免同一世界书条目每轮重复注入，浪费 token
- **配置**：角色设置面板 → 「触发间隔（轮数）」，存入世界书 `cooldown` 字段
- **默认值**：4 轮
- **原理**：注入时记录条目标识（备注名 + 内容前15字符），后续 N 轮内若用户消息中已有该标识则跳过

---

## 四、各字段对照表

| 前端显示 | 数据库/JSON 字段 | 类型 | 说明 |
|----------|------------------|------|------|
| 总开关 | `characters.book_activation` | TEXT | `on` / `off` |
| 条目启用拨片 | `entry.enabled` | bool | `true` = 启用 |
| 激活方式 | `entry.constant` + `entry.selective` | bool+bool | 见上方激活方式表 |
| 主关键词 | `entry.keys` | string[] | 触发关键词列表 |
| 次关键词 | `entry.secondary_keys` | string[] | 选择性模式的二级关键词 |
| 插入位置 | `entry.position` | TEXT | `before_char` / `after_char`（保留字段） |
| 插入顺序 | `entry.insertion_order` | int | 数值越小越先注入 |
| 扫描深度 | `entry.extensions.scan_depth` | int | 保留字段 |
| 触发间隔 | `book.cooldown` | int | 冷却轮数 |

---

## 五、数据流总览

```
用户发送消息
  │
  ├─ buildSystemPrompt()
  │    ├─ 检查 book_activation !== 'off'
  │    ├─ 筛选 constant=true && enabled=true 的条目
  │    └─ 拼入【世界书（常驻）】块 → 系统提示词
  │
  └─ buildApiMessages()
       ├─ 检查 book_activation !== 'off'
       ├─ 筛选 constant=false && enabled=true 的条目
       ├─ 扫描最近8轮对话文本 + 当前输入
       ├─ 主关键词匹配 → (选择性)次关键词匹配
       ├─ 冷却检查（最近N轮已注入的跳过）
       └─ 拼入【参考资料：】块 → 用户消息末尾
```

---

## 六、本次修复内容

### 修复前问题
- `book_activation` 字段存储在数据库但**后端注入逻辑从未检查**——总开关形同虚设
- 前端 `<select>` 有三个选项（关闭/常驻/关键词），但**从未读取或保存**该值
- "激活模式"与条目级"激活方式"功能重叠，语义混乱

### 修复后
- **后端**：两处注入点（常驻 + 关键词）均加 `book_activation !== 'off'` 守卫
- **前端**：3-way `<select>` 改为 on/off 拨片，附带说明文字
- **保存**：`saveCharacter()` 和 `updateBook()` 均发送 `book_activation` 值
- **加载**：`openCharacterModal()` 读取 `book_activation` 并设置拨片状态
- **默认值**：新建角色和 PUT /:id/book 的默认值改为 `off`
- **向后兼容**：旧值 `always`/`keyword` 被 `!== 'off'` 判断视为开启
