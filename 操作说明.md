# AI-GAL 界面与操作说明

这份文档讲**怎么用界面**：主界面每一块是干什么的、菜单里有什么、各项设置怎么填、以及哪些地方容易踩坑。
安装、首次配置 API、生图引擎对接等**部署类**内容请看 [README.md](README.md)。

> 适用版本：桌面版 1.0.2 及以后（含多端界面、访问密码、回顾删单条、CG 实时读入）。

---

## 目录

- [一、两种界面](#一两种界面)
- [二、桌面版主界面](#二桌面版主界面)
- [三、顶栏按钮](#三顶栏按钮)
- [四、左侧游戏列表](#四左侧游戏列表)
- [五、主对话框与推进](#五主对话框与推进)
- [六、行动选项](#六行动选项)
- [七、指令条（输入框）](#七指令条输入框)
- [八、系统菜单](#八系统菜单)
- [九、数据中心](#九数据中心)
- [十、回顾（历史记录）](#十回顾历史记录)
- [十一、幕后控制台](#十一幕后控制台)
- [十二、设置](#十二设置)
- [十三、访问密码](#十三访问密码)
- [十四、移动端界面](#十四移动端界面)
- [十五、快捷键](#十五快捷键)
- [十六、注意事项](#十六注意事项)

---

## 一、两种界面

同一套服务提供两种前端，**按设备自动选择**：

| 界面 | 入口 | 何时进入 |
| --- | --- | --- |
| **桌面版**（全屏 VN 演出界面） | `/` | 电脑浏览器、或带 `?desktop=1` |
| **移动端**（手机 / 平板） | `/mobile.html` | 手机 UA 访问根路径时自动跳转；也可直接打开 |

切换方式：

- 手机上想用桌面界面：访问 `http://<地址>/?desktop=1`（会记住，下次不用再加）
- 桌面想回到移动端界面：访问 `/switch-mobile`
- 桌面版界面里也有「切换到移动端」按钮（顶栏手机图标）

两端**共用同一份数据**（存档、角色卡、名册、CG），只是呈现方式不同。移动端操作见[第十四节](#十四移动端界面)。

---

## 二、桌面版主界面

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏：标题 · 隐藏/显示记录 · AUTO/SKIP/回顾 · BGM · 控制台 · 主题 · 菜单 │
├────┬─────────────────────────────────────────────────────────┤
│ 左 │                                                         │
│ 侧 │                    舞台（CG / 立绘）                      │
│ 栏 │                                                         │
│    │        ┌───────────────────────────────┐                │
│    │        │  行动选项（读到本轮末尾时浮现）  │                │
│    │        └───────────────────────────────┘                │
├────┴─────────────────────────────────────────────────────────┤
│ 主对话框：说话者铭牌 + 头像 + 台词 / 旁白（一次只显示一段）        │
│ 工具行：重画CG · 语音 · 数据中心 · 隐藏UI ／ 回顾 · 段进度 · 上一段·下一段   │
│ （左上角还有「↻ 重新生成」；生成中此处的接缝上会浮出「故事生成中……」）  │
├──────────────────────────────────────────────────────────────┤
│ 指令条：> 输入你的行动或台词…                        [发 送]    │
└──────────────────────────────────────────────────────────────┘
```

从右往左、从上往下依次是：

- **顶栏**：见[第三节](#三顶栏按钮)
- **左侧栏**：角色卡与存档列表，见[第四节](#四左侧游戏列表)
- **舞台**：显示当前场景的 CG。CG 会随着剧情推进自动切换；读到最新段时会跟随最新生成的那张（CG 生成完成后几秒内自动上屏，不需要等下一轮）。
- **主对话框**：一次只演**一段**——对白段显示说话者铭牌与台词，旁白段用另一种字体。演出节奏由你用「下一段」控制。
- **指令条**：你输入行动 / 台词的地方，见[第七节](#七指令条输入框)。

---

## 三、顶栏按钮

| 按钮 | 作用 | 快捷键 |
| --- | --- | --- |
| **隐藏记录 / 显示记录** | 把某些轮次标记为「不进 AI 上下文」，用于压缩上下文、跳过不想要的剧情 | — |
| **AUTO** | 自动推进（按节奏自动点「下一段」） | — |
| **SKIP** | 快速跳过本回合演出 | — |
| **回顾** | 打开历史记录面板 | `L` |
| **BGM 开关** | 播放 / 停止背景音乐（随剧情氛围 mood 自动切歌） | `B` |
| **控制台** | 打开幕后控制台（主 Agent / 管家 AI 的原始处理记录） | — |
| **主题** | 明 / 暗主题切换 | `T` |
| **菜单** | 打开系统菜单，见[第八节](#八系统菜单) | `M` |

> 顶栏按钮都带**悬停提示**：鼠标停上去会说明它是干什么的。

---

## 四、左侧游戏列表

- 顶部 **▶ / ◀** 展开 / 收起侧栏（快捷键 `S`）
- **➕ 新建角色卡**：新建一张空白卡
- **↓ 导入角色卡**：导入 SillyTavern 格式的 `.json` 或内嵌卡 `.png`
- 每张角色卡下面列出**它的存档**（对话），点一下就切换过去
- 每个存档行右侧有两个小按钮：
  - **📥 导出 MD**：把这个存档导出为 Markdown
  - **✕ 删除**：删除该存档（对话记录与缓存文件一起删，不可恢复）

> **重启对话**（顶栏左侧小按钮 ⟲）：回到该角色的开场白，重新开始这一局。

---

## 五、主对话框与推进

- **下一段 ›**（`→` / `空格`）：推进到下一段对白或旁白
- **‹ 上一段**（`←`）：往回看
- 中间显示 **第几段 / 共几段**，旁边的小圆点也是进度指示
- 旁白较长时框内可滚动，右下会出现「↕ 本段较长，可滚动」提示；此时再点「下一段」会先滚到底
- **隐藏 UI**（`H`）：隐藏所有界面元素只看画面（CG / 立绘），再按一次恢复

一回合的文本被切成多段**逐段演出**，读完最后一段才会出现行动选项。

### 文字可以选中复制

主对话框里的正文（对白 / 旁白）**可以用鼠标选中**，方便你复制人名、地名、专有名词 ——
比如行动选项写错了、或你要把某个名字贴到输入框里时，直接拖选复制即可。

> 界面按钮、页签等控件仍保持「不可选中」：那是为了避免点击 / 拖拽时把按钮文字一起刷蓝。

### 对剧情不满意 / 生成出错：点 `↻ 重新生成`

对话框**左上角**有一个 **`↻ 重新生成`** 按钮：

- 点一下 = **把本轮的 AI 回复整体重做一遍**：后端先删掉这一轮的
  **主 AI 正文 + 管家 AI 处理结果**（状态栏、行动选项、记忆、立绘 / CG 判定都在里面），
  以及**本轮生成出来的 CG**，然后按**你上一条发言**重新生成。
- **你自己的发言会保留**，不会被复制成两条，也不需要你重新打一遍。
- 生成期间按钮会置灰（避免误触），结束后恢复。
- 若上一轮**生成失败 / 被中止**（画面上只有红色报错、没有正文），点它同样有效 ——
  这时没有旧回复可删，直接按你上一条发言重跑。

**什么时候用**：

| 情况 | 用哪个 |
| --- | --- |
| 正文格式崩了 / 内容不满意 / 生成报错 | **`↻ 重新生成`**（本轮剧情整个重做） |
| 剧情没问题，只是**画面**不对（构图、人数、画风） | **`↻ 重画CG`**（只重画图，不重跑剧情） |
| 想改 tag 再画（换动作、换场景） | **`↻ 重新生成`**（管家 / 画家 AI 会跟着重走一遍） |

> 功能与[回顾](#十回顾历史记录)里最新一轮的 **↻ 重新生成** 完全一致；
> 对话框左上这个按钮只是把它挪到手边，不必每次都开面板。

### 生成中的两个提示

剧情生成需要时间（主 AI + 管家 AI 可能要几十秒），期间你会看到：

- **鼠标图案变成沙漏** —— 只是图标提示，**点击、滚动、切面板全都照常可用**，功能没有被锁住
- 主对话框与下方输入框之间浮出一条 **「故事生成中……」** 提示条；生成结束自动消失

想中途放弃就点右下角的 **停止**（生成中「发送」会变成「停止」）。

### 对画面不满意：点 `↻ 重画CG`

对话框右上角、「语音」的左边有一个 **`↻ 重画CG`** 按钮：

- 点一下，后台会把**当前这张 CG 的原始生图指令重新发一次**，生成一张新的
- **旧图会被新图覆盖在它原来的位置**：你点的是第几张，新图就落在第几张 ——
  不会跑到列表最前面（最新）或最后面去，其它 CG 的位置也不受影响
- 生成需要数十秒，完成后画面**自动换成新图**（不用手动刷新，也不用等到下一轮）
- 舞台上正显示的那张就是「当前 CG」；即使你在回顾旧 CG，重画的也是你正在看的那张
- CG 画廊面板里，**每张 CG 右上角的 `↻`** 是同一个功能，而且同样只覆盖**你点的那一张**

**什么时候用**：构图不对、人数不对、画风不喜欢、角色特征画错 —— 直接点它重来一张。

> 若想**改动 tag 再画**（换动作、换场景），那是「重新生成本回合」，
> 用[回顾](#十回顾历史记录)里的 **↻ 重新生成** 更合适（它会带着剧情重走一遍管家 / 画家 AI）。

### 对头像不满意：删掉名册里的 avatar 链接

角色头像（立绘）不走按钮重画，而是**删掉名册里的头像链接** —— 系统读不到链接就会重新生成：

1. 找到该存档目录下的名册文件：

   ```
   <存档目录>\character_roster.json
   ```

   （桌面版存档在 `%APPDATA%\AI-GAL\saves\game0001\<存档ID>\`）

2. 用文本编辑器打开，找到要换的**那个角色名**，把它的 **`avatar` 字段清空**（改成 `""`）：

   ```json
   {
     "黑崎静": {
       "avatar": "",
       "发色": "black_hair",
       "瞳色": "blue_eyes"
     }
   }
   ```

   也可以直接删掉 `"avatar": "xxx.jpg",` 这一行。

3. 保存文件，回到游戏**继续下一轮对话**。管家 AI 比对名册时会发现这个角色**没有头像**，
   于是重新为它生成一张。

> ⚠️ **只改 `avatar` 的值，不要改角色名**（键名必须与别处一致）；改前建议先备份这个 json。
> 「当前存档换头像」改当前存档目录里这份；想连总存档（多个存档共用的头像来源）一起换，
> 则要改总存档目录里对应的那份。

---

## 六、行动选项

行动选项是**本回合可选的行动**，由管家 AI 根据剧情生成。

- **只有推进到本回合最后一段时**，选项才会浮现在舞台中央
- 往回翻、或查看历史时自动收起，不占用画面
- 点某个选项 = 把它作为你的发言发送出去，开始下一回合
- 你也可以**不选**，直接在指令条里自己写行动或台词

---

## 七、指令条（输入框）

- 输入你的行动或台词，按 **Enter** 发送
- **Shift + Enter** 换行
- **Esc** 收起面板 / 关闭弹窗
- 输入框旁会显示当前可用的输入提示

输入框支持 **STscript 斜杠命令**（SillyTavern 脚本语言兼容），常用几个：

| 命令 | 作用 |
| --- | --- |
| `/hide` `/unhide` | 隐藏 / 恢复某轮不进上下文（也可用顶栏按钮） |
| `/trigger` `/regenerate` `/swipe` `/continue` | 重新生成 / 续写 |
| `/bg` `/lockbg` `/unlockbg` | 设置 / 锁定背景 |
| `/bubble` `/flat` `/single` | 切换对话气泡样式 |
| `/inject` `/note` | 注入提示词 / 写作者备注 |
| `/roll` `/random` | 掷骰 / 随机 |
| `/help` | 列出全部命令 |

完整命令表（变量、条件、循环、世界书、角色操作等）在代码 `public/js/stscript.js` 头部注释里，输入 `/help` 也会列出来。

---

## 八、系统菜单

点顶栏 **≡ 菜单**（或按 `M`）打开。菜单按用途分组：

### 账号与接口

| 项 | 说明 |
| --- | --- |
| **用户设定** | 多用户与扮演身份：昵称、头像、人设 |
| **API 供应商与预设** | 增删改 API 供应商、选模型、导入 / 导出 ST 预设 |

### 故事

| 项 | 说明 |
| --- | --- |
| **游戏列表** | 展开左侧栏（角色卡 / 切换存档） |
| **游戏存档** | 同样打开左侧栏，用于新建 / 导出 / 删除存档 |
| **重启对话** | 回到开场白重新开始 |
| **导出对话** | 导出为 Markdown / JSON |

### 数据中心

打开右侧数据面板并定位到对应页：**主要状态** / **角色名册** / **CG 画廊** / **世界状态 (MVU)**。

> 菜单里这四项是**快捷定位**；面板本身共有五个页签，比菜单多一个 **记忆表格**，见[第九节](#九数据中心)。

### 声音与外观

- **语音朗读（TTS）**：进入系统级 TTS 设置（关闭后对话框上的语音开关会置灰）
- **字号**：滑块 / `A−` `A＋` / 小·标准·大·特大预设（`Ctrl` + `−` / `＋`）
- **BGM 开关**

### 安全

| 项 | 说明 |
| --- | --- |
| **访问密码** | 设置 / 重置远程访问密码，见[第十三节](#十三访问密码)。副标题会实时显示状态（如「⚠ 仍是默认密码 12345」） |

### 调试

| 项 | 说明 |
| --- | --- |
| **幕后控制台** | 打开主 Agent / 管家 AI 的处理记录 |

菜单底部有 **↩ 返回游戏**。

---

## 九、数据中心

从菜单进入，或用快捷键 `D`，也可以在对话框工具行点各入口。左侧是五个页签：

| 页签 | 内容 |
| --- | --- |
| **主要状态** | MVU 变量、数值条（角色状态、好感度等） |
| **角色名册** | 登场角色：头像、性别、年龄、体貌、关系、好感度 |
| **CG 画廊** | 本存档的所有 CG，点图放大；每张右上角有 **↻ 重新生成** 与 **✕ 删除** |
| **记忆表格** | 每轮一句的剧情摘要，累积成表。上方有三色灯图例（见下） |
| **世界状态** | MVU 世界变量：播种 / 重放 / 清除 |

> **CG 画廊**里的 CG 会自动成为舞台背景；新 CG 生成完成后几秒内，**面板与舞台都会自己更新**，
> 不需要手动刷新、也不用切走再切回来（早期版本里这个面板只在打开时画一次，容易让人以为"图没进画廊"）。

### 记忆表格与三色灯

每一轮结束后，**管家 AI** 会把该轮剧情浓缩成一句话（时间 / 地点 / 人物 / 事件），
**立即追加写入存档目录下的 `event_log.md` 文件**。

⚠️ 关键点：**写入文件不等于进入上下文**。平时这段记忆只躺在文件里，
**只有每到设定轮次时**（默认第 20、40、60… 轮），系统才把**目前累积的完整表格**注入一次上下文。
一旦注入，它就会一直留在上下文里（不会因为对话变长而被挤掉），直到下一次注入用更新的表格替换它。

记忆表格页签顶部是**图例**，每条记忆前面有一个灯：

| 灯 | 含义 |
| --- | --- |
| 🟡 **黄灯 · 已登记** | 该轮摘要已写入 `event_log.md`，但还没被任何一次注入带进上下文 |
| 🟢 **绿灯 · 已注入** | 该轮摘要已包含在注入的表格中，AI 现在能看到它 |
| 🔴 **红灯 · 内容缺失** | 该轮没有摘要，或摘要为空（写入失败）。可点该条目手动补写 |

> 首次打开某存档时，早于首次注入的轮次会显示**黄灯**，注入后变**绿灯**，属正常现象。

#### 记忆表格与「重新生成」的关系

**重新生成某一轮时，系统会连带清掉那一轮在 `event_log.md` 里的记忆行**，
再用新剧情重新写一行 —— 所以表格里不会留下被替换掉的旧摘要（同一轮永远只有一行）。

如果摘要被丢弃（比如管家把一大段思考过程当成了摘要，或摘要超长），
那一轮会**没有记忆行**、在记忆表格里显示为**红灯 · 内容缺失**，可以点该条目**手动补写**。
这是刻意的：宁可显示"缺失"，也不把一段和剧情无关的文字塞进表格污染后续上下文。

> **老存档如果已经出现重复或错位的行**（同一轮号出现两次、或表格轮号与剧情对不上），
> 可以点记忆条目手动改；也可以用仓库里的修复脚本
> `tools/repair-event-log.js`（默认只报告、加 `--apply` 才写入，写前自动备份）批量修。

### 忽略之前的对话（长局续接）

每到**第 M 轮**（默认 60，必须是注入间隔 N 的整数倍），系统会弹窗询问：

> 已在第 60 轮注入完整记忆表格。是否忽略之前的对话，只保留记忆表格？

- 选 **忽略并只保留表格**：上下文改为「第 1~60 轮的记忆表格 + 第 60 轮及之后的完整对话 + 一段说明」，
  第 1~59 轮的对话原文不再送进 AI。适合长局省 token —— 表格已经涵盖了早期的剧情。
- 选 **暂不忽略**：保持完整历史，下次到 M 的倍数轮还会再问一次。
- **不会删除任何消息**，随时可以在记忆表格页签顶部的黄色横幅点 **撤销** 恢复完整历史。

相关设置在 **系统菜单 → 设置 → 记忆表格**：
- **注入间隔 N**：默认 20，即第 20、40、60… 轮各注入一次；
- **启用「忽略之前的对话」提示**：关掉就不会再弹窗；
- **提示轮次 M**：下拉选择，只会列出 N 的整数倍（改了 N，选项自动跟着变）。


---

## 十、回顾（历史记录）

顶栏 **回顾** 或按 `L` 打开。用于查看已发生的每一轮，并做局部修正。

- 按轮次分组，每行显示轮次、摘要、段数
- 顶部可按 **全部 / 对白 / 旁白** 过滤，**展开全部 / 收起全部**
- 点某一行展开，里面每段都有 **跳到这段 ›**（把舞台定位回那一刻）
- 展开后有两个操作按钮（针对**这一轮的 AI 回复**）：
  - **🗑 删除这条回复**：删掉这一轮的 AI 回复，**连同该轮的管家 Agent 处理记录**（幕后控制台里对应的思维链 / portrait / CG 判定卡片）一起清掉；**你自己那句发言会保留**
  - **↻ 重新生成**：只有**最后一轮**才有。先删旧回复、再用同一句发言原地重新生成（不会留下重复的用户楼层）

**什么时候用**：某一轮生成得不好（格式崩了、内容不满意）时，用「重新生成」重来一条即可，不必删掉整个对话。

> 「重新生成」只对最后一轮开放——中间轮次的上下文已被后续剧情改写，重发出来的结果不会与后面的内容自洽。

---

## 十一、幕后控制台

顶栏 **控制台** 打开。这是**排查问题**用的面板，显示每一轮 AI 的原始处理记录：

- **主 Agent**：完整输出、解析后的 JSON、思维链（Chain-of-Thought）
- **管家 AI**：处理结果、修复后的全文（fixedText）、思维链、发给它的 System / User Prompt

点任一条目可看**完整内容**（不截断），右上角可 **复制**。

> 生成结果异常时，先来这里看：是主 AI 没按格式输出，还是管家没修好，一目了然。

---

## 十二、设置

顶栏齿轮（或菜单里的「用户设定 / API 供应商与预设」）进入。

### 用户设定（身份）

- 多用户：可新建 / 删除用户，各自独立的昵称、头像、人设
- **昵称与人设**会作为 `{{user}}` 注入提示词，影响 AI 对你的称呼

### AI 与供应商

四类角色**各自独立配置**，可以指向完全不同的服务商与模型：

| 角色 | 职责 | 建议模型 |
| --- | --- | --- |
| **主 AI** | 写剧情正文（旁白 / 对白 / 状态 / 行动） | 越强越好 |
| **管家 AI** | 格式修补、氛围判定、行动选项、记忆摘要、生图触发判定 | 便宜的小模型即可 |
| **画家 AI** | 把剧情转成生图提示词（立绘 / CG） | 同上 |
| **代理** | 网络代理设置（访问受限的 API 时用） | — |

每个供应商需要填：名称、类型、`base_url`、API Key、模型名。旁边有 **拉取 / 获取模型** 按钮可验证连通性。

- **导入 ST 预设 / 编辑预设参数**：兼容 SillyTavern 聊天补全预设
- **导出 ST 预设**：导出为标准 SillyTavern 预设（**含当前 API 供应商配置，但不含 API Key**）
- **设为默认 / 删除**

### 图像生成

- **模式**：`anima`（默认，极简本地生图）/ `comfyui` / `openai` / `stability` / `none`
- **画风与质量前缀**：头像与 CG 各自的质量前缀（可含艺术家信息）
- **工作流文件**：ComfyUI 用的工作流 JSON（放在项目根目录）
- **工作流节点 ID**：一般留空自动探测
- **负向提示词**：留空用工作流自带的

#### 图片尺寸（按引擎分开，互不影响）

| 引擎 | 尺寸怎么设 |
| --- | --- |
| **anima-turbo-cg** | 下拉里是**本地模型推荐档**：`1024x1024`（原生，推荐）/ `768x768`、`512x512`（纯 CPU、低显存）/ `832x1216`、`1216x832`、`1024x1536`、`1536x1024` |
| **OpenAI 兼容 / Stability** | 下拉里是**云端服务商的合法尺寸**（DALL·E 3 / SenseNova 等：`1024x1024`、`1792x1024`、`2048x2048`…） |
| **NovelAI** | 下拉里是官方常用档（宽高须为 64 的倍数；>1024² 或 >28 步会消耗 Anlas） |
| **ComfyUI** | 尺寸由**工作流 JSON**决定（本页的尺寸栏对它无效） |

- 三个引擎的尺寸菜单**完全分开**：本地档不会出现在云端菜单里，反之亦然。
- 需要别的尺寸就选 **「自定义（宽 × 高）…」**，然后在**「宽」「高」两个格子里分别填数字**
  （不再需要手写 `1024x768` 这种带 x 的字符串 —— 以前写成大写 `X`、全角 `×`、`*`
  会被本地引擎**静默忽略**，出图回落到默认尺寸，看起来就是"分辨率设置没生效"）。
- AI-GAL 保存与发送时都会把尺寸规范化成 `宽x高`（小写 x）；本地引擎还会把宽高对齐到
  8 的倍数、夹在 256~3072 之间，超出推荐范围（512~1536）时在服务端日志里给出提示。

### 外观与界面

- 主题（明 / 暗两套）
- 自定义主题色、导出 / 导入主题、重置为默认
- 字号、界面元素显隐

### 语音（TTS）

- 供应商：火山引擎 / 阿里百炼 / SiliconFlow / Volink / ComfyUI Qwen3-TTS 等
- 可测试朗读、选择音色
- 关闭后对话框上的语音开关会置灰

---

## 十三、访问密码

用于**保护局域网 / 外网访问**。规则：

| 场景 | 是否需要密码 |
| --- | --- |
| **本机访问**（运行 AI-GAL 的那台电脑） | **不需要**，直接进入 |
| **非本机访问**（同一局域网的其他设备、外网） | **整个界面都需要密码** |

### 设置方法

1. 在**运行 AI-GAL 的电脑上**打开界面（本机访问免密）
2. 顶栏 **≡ 菜单 → 安 全 → 访问密码**
3. 输入新密码（至少 4 位）并确认 → **保存密码**

### 默认密码与首次提示

- **默认密码是 `12345`**
- 仍是默认密码时：菜单里那一项会显示 **「⚠ 仍是默认密码 12345」**，本机首次进入时也会弹一次提示

### 忘记密码怎么办

到**运行 AI-GAL 的那台电脑上**打开界面，用同一个入口：
- 输入新密码保存，或
- 点 **重置为默认 12345**

> 改密码 / 重置**只能在主机上做**。从其他设备打开时，这个弹窗的输入框会被禁用并提示你到主机操作——这是有意为之：否则知道默认密码的人就能改掉你的密码。

### 登录

非本机访问时会先看到登录页：输入密码 → **进入**。登录状态会记住 30 天，期间不用重复输入。连续输错过多会被短暂限速。

---

## 十四、移动端界面

手机 / 平板访问根路径会自动进入；也可直接打开 `/mobile.html`。

底部五个页签：**对话 · 状态 · 名册 · 画廊 · 菜单**。

- **对话页**：与桌面版相同的主对话框（一次一段）+ 指令条
  - **行动选项**同样只在**读到本回合最后一段时**才浮现在画面中央，不占常驻空间
  - 底部有 ‹ 上一段 / 下一段 ›、回顾、朗读
  - 桌面版对话框左上的 **`↻ 重新生成`** 与生成中的沙漏 /「故事生成中……」提示**只有桌面版有**；
    移动端要重做某轮，请用 **回顾 → ↻ 重新生成**（功能相同）
- **状态 / 名册 / 画廊**：对应桌面版的数据中心页
  - **画廊里的 CG 是缩略图**，点按可全屏看大图
- **菜单页**（最后一页签）把常用入口收在一起：
  - 游戏列表 / 游戏存档 / 导入角色卡 / 导出对话
  - 主要状态 / 角色名册 / CG 画廊 / 记忆表格 / 世界状态
  - 语音朗读 / BGM 开关 / 切换 AI 供应商 / AI 供应商与预设 / 用户设定
  - 幕后控制台

移动端同样受[访问密码](#十三访问密码)保护；从非本机访问需要先登录。

---

## 十五、快捷键

桌面版界面（聚焦在游戏界面时）：

| 键 | 作用 |
| --- | --- |
| `→` / `空格` / `PageDown` | 下一段（推进剧情） |
| `←` / `PageUp` | 上一段 |
| `M` | 打开 / 关闭菜单 |
| `L` | 回顾（历史记录） |
| `H` | 隐藏 / 显示界面（只看画面） |
| `T` | 切换明 / 暗主题 |
| `D` | 数据面板 |
| `B` | BGM 开关 |
| `V` | 语音朗读开关 |
| `S` | 展开 / 收起左侧游戏列表 |
| `I` | 聚焦到指令条 |
| `Esc` | 关闭当前弹窗 / 收起面板 |
| `Ctrl` + `−` / `＋` | 缩小 / 放大正文字号 |

---

## 十六、注意事项

### 安全

- ⚠️ **开放局域网前先改掉默认密码**。默认密码 `12345` 是公开的；菜单里会一直提醒你。
- 局域网模式下，同一网络内的设备都能访问界面。**不要在不信任的网络里开放**（例如公共 Wi-Fi）。
- 本机访问永远免密——**别把"本机能打开"当成安全**，任何能在你电脑上操作的人都能进。
- 桌面版的**局域网开关默认关闭**，要用时通过开始菜单的「AI-GAL（局域网模式）」快捷方式启动，或在托盘设置里勾选。

### 数据

- 存档默认在 `%APPDATA%\AI-GAL`（桌面版），包含数据库、存档目录、上传的图片。
- **升级 / 覆盖安装不会丢失存档**，但**手动删除 `%APPDATA%\AI-GAL` 会**。
- 删除存档 / 删除单条回复**不可恢复**，操作前想清楚。
- 接入第三方 API 时，剧情与角色设定会发送给对应服务商，请自行判断内容是否合适。

### 生成质量

- **主 AI 决定文笔与剧情质量**，别在这上面省钱；管家 / 画家用便宜模型即可。
- 生成结果格式不对时，先去**幕后控制台**看是主 AI 输出问题还是管家修复问题。
- 某一轮生成不好：用对话框左上的 **↻ 重新生成**（或**回顾 → 重新生成**）重来一条，不要删整个对话。
- 生图较慢（尤其本地 CPU 推理）时，CG 会在生成完成后**自动出现**，不需要手动刷新。

### 生成出错：模型只"想"不"说"（输出预算被思维链吃光）

**现象**：点发送 / 重新生成后，主对话框里出现一段报错，或（老版本）直接把一大段**英文思考稿**
当成剧情显示出来，文字是 `The user wants me to…` / `Let me think…` 这类，而不是中文剧情。

**原因**：你用的是**思考模型（thinking / reasoning model）**，它先输出一大段思维链、再写正文。
如果「最大输出长度」太小，思维链就会把预算**全部用光**，正文一个字都写不出来 ——
这是模型＋参数的组合问题，不是角色卡或剧情哪里坏了。重新生成也没用（同样的请求会得到同样的结果）。

**怎么修**（任选其一，改完立刻见效）：

| 做法 | 位置 | 说明 |
| --- | --- | --- |
| **取消勾选「启用思考模式」** | 设置 → **AI 与供应商** → 编辑该供应商 | **只对本地引擎生效**（llama.cpp / Ollama 等 `127.0.0.1` / `localhost` / 局域网地址）：服务端会自动改用 `enable_thinking=false` |
| **把「最大输出长度」调大** | 同上 | 对**所有**供应商都有效。思考模型建议 **8192 ~ 16384**（思维链常常一次就吃掉好几千 token） |
| **换一个不输出思维链的模型** | 同上 | 远程供应商最省事的解法 |

> **远程供应商（`https://…`）注意**：服务端**不会**给它发送"关闭思考"的未知参数（很多网关遇到
> 不认识的字段会直接 400 报错），所以「启用思考模式」这个勾选框对远程供应商**不起作用** ——
> 远程只能用「调大最大输出长度」或换模型。报错信息里也会这样提示你。

**服务端会自动做的补救**：如果主 AI 是**本地引擎**、第一次把预算全花在思维链上（`finish_reason=length`
且正文 0 字），服务端会**自动关闭思考模式重试一次**；救回来时会弹一条提示告诉你。
若重试后仍然没有正文，会显示一条**带具体原因与改法**的报错，并且**不会**把思维链当成剧情存进存档。

### 前端与缓存

- 界面文件（HTML / CSS / JS）更新后，浏览器**刷新页面**即可；若样式没变化请**硬刷新**（`Ctrl + Shift + R`）。
- 服务端改动（`server/**`）需要**重启服务**才生效。

---

## 相关文档

- [README.md](README.md) —— 功能特性、安装部署、首次配置 API 与生图引擎
- [WORLDBOOK_ACTIVATION_GUIDE.md](WORLDBOOK_ACTIVATION_GUIDE.md) —— 世界书三种激活模式的用法
