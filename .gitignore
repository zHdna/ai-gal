# ---- 依赖库：随仓库分发（请勿排除）----
# node_modules/ 是「即下即用」的前提：内置便携版 Node 22（runtime/node.exe）与之 ABI 匹配，
# 用户既不需要、也无法执行 npm install（包内不含 npm）。
# 因此这里**故意不忽略** node_modules/。若确需临时排除，请用 git add -f 显式添加。

# ---- 用户数据（never share）----

# SQLite database + WAL sidecars + per-install encryption secret
server/db/data.db
server/db/data.db-shm
server/db/data.db-wal
server/db/.crypto_secret
*.db
*.db-shm
*.db-wal

# Player saves, generated images, audio cache, uploads
saves/
data/generated_images/
data/tts-cache/
data/tts-cache-bak/
data/tts-queue.json
data/thumbs/
server/generated_images/
public/uploads/

# Character profile art / NPC portraits
profile/

# 桌面版/本地运行时写出的机器相关配置（端口等），不应入库
# （server/desktop-config.js 会在 DATA_ROOT 下读写它；从源码直接跑时会落在仓库根）
desktop-config.json

# ---- Logs & scratch ----
*.log
*.out
*.err
.probe-tmp/

# ---- 本机会话记忆（含本机绝对路径与私有笔记）----
# AGENTS.local.md / AGENTS-ARCHIVE.local.md 是**每台机器各自的**记忆文件，绝不入库。
# 这条必须写在仓库自己的 .gitignore 里：只靠用户级全局 ignore（~/.config/git/ignore），
# 换一台机器克隆下来它们就会变成未跟踪文件，一次 `git add -A` 就会连本机路径一起推上去。
*.local.md

# ---- 桌面版构建产物 & 工具链 ----
# desktop/ 的源码要提交；下面是它的依赖与产物，不入库。
desktop/node_modules/
dist-desktop/

# ---- OS / editor ----
Thumbs.db
Desktop.ini
.DS_Store
*.lnk
